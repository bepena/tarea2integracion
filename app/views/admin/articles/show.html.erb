admin
<h1><%= @article.title %></h1>
<h3><%= @article.subtitle %></h3>
<p><%= @article.body %></p>

<h3>Comentarios</h3>
<br>
<% @article.comments.each do |c| %>
<%= c.author %> :
<%= c.content %> |
<%= link_to 'Editar', edit_admin_article_comment_path(c.article,c) %>
<%= link_to 'Eliminar', [c.article, c], method: :delete,
data: { confirm: 'Are you sure?' } %>

<br>
<%end%>





  <%= link_to 'Atras', admin_articles_path %>
