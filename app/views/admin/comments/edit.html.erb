<%= form_for([@comment.article, @comment]) do |f|%>
  <%= f.label :nombre %>
  <%= f.text_field :author %>

  <%= f.label :comentario %>
  <%= f.text_field :content %>
  <%= f.submit %>

  <%end%>

  <%= link_to 'Atras', admin_article_path(@comment.article) %>
